const config = {
    backgroundColor: '#e2eede',        // 键盘后方颜色
    borderColor: '#ddd',               // 按键的边线颜色
    contentColor: '#fff',              // 按键的填充颜色
    fontColor: '#333',                 // 按键的字体颜色
    shadowColor: 'rgba(0, 0, 0, 0.1)', // 按键的阴影颜色
};

export default { config };
